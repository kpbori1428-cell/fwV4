/**
 * Editor — Paleta Tarjeta
 * (Contenedor vacío hasta que se definan las funciones PLUS)
 */

export function construirControles(datosPlus, elemento, onCambio) {
    const contenedor = document.createElement('div');
    return contenedor;
}
